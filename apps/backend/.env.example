# Backend — override Config.yaml values at runtime
# Use dot notation matching the YAML key paths (lowercase)

clerk.secretKey=
clerk.jwtKey=
database.url=postgresql://postgres:postgres@localhost:5432/jordan_devs

server.port=3001
server.nodeEnv=development
server.frontendOrigins=http://localhost:3000
clerk.authorizedParties=http://localhost:3000
initialAdminClerkUserId=
