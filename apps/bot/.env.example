# Bot — override Config.yaml values at runtime
# Use dot notation matching the YAML key paths (lowercase)

discord.token=
discord.prefix=!
discord.devGuildId=
owners.ids=
owners.devIds=
github.token=
scanning.virustotalApiKey=
scanning.cacheTtlMs=300000
scanning.rateLimit=10
scanning.timeoutMs=10000
scanning.maxRedirects=10
database.driver=sqlite
database.url=file:data/bot.sqlite
